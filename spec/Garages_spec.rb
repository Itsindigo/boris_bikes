require 'Garages.rb'

describe Garages do
	#context "mock of a van with two broken bikes in." do
	#	let(:van) { double(:van, :take_broken => [bike1 = false, bike2 = false])}
#
#		it 'fixes broken bikes' do
#			allow(van).to receive(:to_fix)
#			expect(subject.to_fix(van.take_broken)).to eq [true, true]
#		end
#
#		it 'ensures same amount of bikes are returned' do
#			expect(subject.to_fix)
#		end
#	end
#
end